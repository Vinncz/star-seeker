import Foundation

struct AppConfig {
    
    /** Dictates whether the project is in debug mode or not. Probable sensitive information is revealed during debug mode, so make caution. */
    static let debug            : Bool = false
    
    static let delimiter        : String = ";"
    static let newLineCharacter : String = "\n"
        
}
